apiVersion: batch/v1
kind: Job
metadata:
  name: watermark-job
spec:
  template:
    spec:
      containers:
      - name: watermark
        image: deweihou/watermark-app:latest
      restartPolicy: Never
  backoffLimit: 3